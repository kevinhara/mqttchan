node_modules
**/node_modules
**/dist
.env
# Must be excluded alongside dist/: tsc --build reads these and would conclude
# everything is already compiled, emitting nothing into an image that has no
# dist/ at all. The failure is silent - the build succeeds and the image is
# empty of JavaScript.
**/*.tsbuildinfo
