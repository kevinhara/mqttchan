{
  "name": "mqttchan-services",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "api", "feeds/*"],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "typecheck": "tsc --build --force",
    "test": "vitest run",
    "dev:api": "tsx watch api/src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^5.0.1"
  }
}
